name: 'brandon_databricks_issue'
version: '1.0.0'
config-version: 2

require-dbt-version: [">=1.12.0", "<1.13.0"]

profile: 'brandon_databricks_issue'

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  brandon_databricks_issue:
    +schema: landing_crm